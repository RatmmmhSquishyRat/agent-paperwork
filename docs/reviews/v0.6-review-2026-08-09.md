# Review Book — v0.6 (CLI Grammar: Named-Flag Rewrite) · QA 全量实测

**Date**: 2026-08-09
**Scope**: v0.6 CLI 文法（位置参数仅剩 PATH、必填参数一律具名 flag）的独立 QA 全量实测
**规格基线**: `docs/ssot/specs/cli-grammar-v0.6/{spec,design,bdd,tdd,impl_plan}.md`
**实测对象**: worktree `agent-paperwork-wt-v06grammar` 分支 `cli-grammar-v0.6`，HEAD `902fe31`，`cargo build --release` 产物
**实测环境**: Windows 25H2 / PowerShell 7 / zh-CN locale（locale 与 BUG-1/BUG-2 的表型相关，见 §7）
**Previous review**: [v0.5-review-2026-08-09.md](./v0.5-review-2026-08-09.md)
**角色约束**: 独立 QA——只做构建、运行与临时文件实测，不改任何既有文件；本轮不发布（spec §7.4），判定口径为「可供 owner 验收的稳定候选」。

---

## 1. 架构 delta：v0.5 → v0.6 变更面清点

### 1.1 提交清点（基线 e71f4ca 之后的 9 提交）

| 提交 | 主题 | 变更面 |
|---|---|---|
| `42c6d34` | docs(governance) | v0.6 规格五文档 + 四份 doc review + v0.6_feedbacks + role 落盘（纯文档） |
| `5456eed` | refactor(core) | ops/*.rs 4 文件 19 处 example/fix 字符串换 v0.6 具名文法（纯文案，API 零变更）——对应 spec §6「core example 文案」行 |
| `a07ad4c` | merge | master（format-v2：post create 删除、send 建线程、core v2 格式）合入本分支；按 owner 追裁 D1/D2，`--to`/`--participants` 未随合并进入 |
| `b478a57` | docs(corrigendum) | 基线勘误：spec/tdd/impl_plan 删除 `--to`/`--participants` 残留，tdd 1b 以合并基线重新清点（纯文档） |
| `d6e9ff3` | feat(cli) | 五个 cmd 文件（post/profile/brief/contacts/validate）全量具名文法：`--author/-a`、`--message/-m`、`--seq`、`--name`、`--title`、`--entry`、`--entry-title`、`--profile`；`allow_hyphen_values` 落 `--message`（98+/73-） |
| `14699ea` | refactor(cli) | usage 信封静态规范示例全换 v0.6 具名形态；`--` 边界教学废止（main.rs 39+/61-）——对应 spec §5 差异 2/3 |
| `c31c4cf` | test(cli) | 集成套件重写为 v0.6 文法（tdd 1b 翻转 + tdd §4 新用例，253+/127-） |
| `15d0c30` | ci(smoke) | CI smoke 换 v0.6 文法 + edit smoke + 旧文法 usage 信封断言 |
| `902fe31` | docs | README/SKILL 示例刷新 v0.6；adr-v1 加 superseded-by 注记 |

### 1.2 与 spec 对照的 delta 核验结论

- **变更面边界与 spec §6/§7 一致**：破坏面仅限命令参数文法；`paperwork-core` 公开 API 签名、四类文件格式、信封结构、七类 category、`--json/--plain/-q/-V`、ensure_suffix、别名全部冻结（本轮实测未发现越界改动，见 §4）。
- **版本纪律合规**：`paperwork -V` 仍输出 `paperwork 0.5.0`——未 bump、无 tag、无 CHANGELOG 发布段、无发布动作（spec §7.4 约束满足）。
- **互斥错误层级提升落地**：`--message`/`--stdin` 两形态（同给/皆缺）均由 clap 层判定 usage exit 2（spec §5 差异 1），实测符合（§3）。
- **一处越界**：`d6e9ff3` 在 `post.rs` 的 clap doc 注释引入 2 处 `§`（U+00A7），进入 `post send --help` stdout，击穿冻结的纯 ASCII 输出契约——见 §7 BUG-1。

---

## 2. 文法全表实测（spec §2 逐签名）

64 条实测（G01–G64），全部真实执行；G26/G31 首次因 QA 侧参数理解偏差失败，修正后复测通过，不计为产品缺陷。

### 2.1 post 组

| 用例 | 命令形态 | 期望 | 实际 | 判定 |
|---|---|---|---|---|
| G01 | `post send <PATH> --author alice --message "Hello v0.6"` | exit 0 | `ok post.send #1` | PASS |
| G02 | 短形式 `-a alice -m "short forms ok"`（等价性） | exit 0，与长形式等价 | `ok post.send #2` | PASS |
| G03 | `--stdin`（多行 stdin 正文） | exit 0 | `ok post.send #3` | PASS |
| G04 | `--reply-to 2`（糖衣 flag） | exit 0，token 注入 | `ok post.send #4`（落盘见 §5） | PASS |
| G05 | `--mention bob,carol`（逗号分隔） | exit 0 | `ok post.send #5` | PASS |
| G06 | 新线程 + `--title "My New Topic"` | exit 0 建线程 | `ok post.send #1`（preamble 核对见 §5） | PASS |
| G07 | `-m "-dash body no -- needed"`（allow_hyphen_values，无需 `--`） | exit 0 | `ok post.send #6` | PASS |
| G63 | `-m -a`（值恰为短 flag 字面） | exit 0 值直传 | `ok post.send #3` | PASS |
| G64 | `-m --stdin`（值恰为 flag 字面） | exit 0 值直传 | `ok post.send #4` | PASS |
| G09 | `post edit --author bob --seq 6 --message ...` | exit 0 | `ok post.edit #6` | PASS |
| G10b | edit 经 `--stdin` 通道 | exit 0 | `ok post.edit #1` | PASS |
| G12–G16 | read 全 flag：`--from`/`--to`/`--mention`/`--reply-to`/`--limit` | exit 0，窗口正确 | `--from 2 --to 4` 得 3 条；`--mention bob` 得 1 条；`--reply-to 2` 得 1 条；`--limit 2` 得 `showing: 2/6 window: #5-#6` | PASS |
| G17 | `post summary` | exit 0 | title/participants/messages/last.* 字段齐 | PASS |
| G61 | edit 三重护栏：非本人消息 | not-allowed exit 1 | `error not-allowed` | PASS |
| G62 | edit 三重护栏：非线程末条 | not-allowed exit 1 | `error not-allowed` | PASS |

### 2.2 profile / brief / contacts / validate 组

| 用例 | 命令形态 | 实际 | 判定 |
|---|---|---|---|
| G18 | `profile create --name alice --model --description` | exit 0 | PASS |
| G19 | `profile create --name bob --scope-read/write/owns` | exit 0 | PASS |
| G20–G22 | `profile show/edit/list` | exit 0（list 报 2 profiles） | PASS |
| G24 | `brief create --title --owner --description` | exit 0 | PASS |
| G25 | `brief add --entry <path> --note` | exit 0，hash 快照落盘 | PASS |
| G26b | `brief add --entry <path> --regex "fn main"` | exit 0 | PASS |
| G27/G28 | `brief read [--full]` | exit 0，full 显 64 位 hash | PASS |
| G29/G30 | `brief verify [--base-dir]` | exit 0，`1/1 fresh` | PASS |
| G31b | `brief remove --entry-title alice.profile.md` | exit 0 | PASS |
| G32/G33 | `contacts create [--title]`（默认 `Contacts`） | exit 0 | PASS |
| G34/G35 | `contacts add --profile`、`contacts read` | exit 0 | PASS |
| G36/G37 | `validate [--type profile]` | exit 0 | PASS |

### 2.3 负向文法

| 用例 | 形态 | 期望 | 实际 | 判定 |
|---|---|---|---|---|
| G08 | send 空 author（`--author "   "`） | validation exit 1 | `error validation: sender name (--author) is empty` | PASS |
| G11 | edit `--seq abc`（非数字） | usage exit 2 | usage 信封 | PASS |
| G38 | `validate --type banana` | usage exit 2（possible values 约束） | usage 信封 | PASS |
| G39 | read `-m bob`（`--mention` 无短形式，避让 `-m`） | usage exit 2 | `unexpected argument '-m'` | PASS |
| P14 | read `-r 2`（`--reply-to` 无短形式） | usage exit 2 | usage 信封 | PASS |
| P15 | `brief read -f`（`--full` 无短形式） | usage exit 2 | usage 信封 | PASS |
| G40 | send 互斥同给（`-m x --stdin`） | usage exit 2（层级提升） | clap conflicts → usage | PASS |
| G41 | send 互斥皆缺 | usage exit 2 | MissingRequiredArgument → usage | PASS |
| G42 | `--message "   "`（trim 后空） | validation exit 1 | `message body is empty` | PASS |
| E16 | `--stdin` 空 stdin | validation exit 1 | `message body is empty` | PASS |
| G43 | 旧 v0.5 位置文法 `send <PATH> alice "Hi"` | usage exit 2 | `unexpected argument 'alice'` + v0.6 规范示例 | PASS |
| G44 | 旧 `--from` 身份 flag 于 send | usage exit 2 | 未知 flag + 迁移教学 fix | PASS |
| G45–G51 | 各命令缺必填 flag（send author、edit seq、profile name、brief title/entry/entry-title、contacts profile） | usage exit 2 ×7 | 全部 usage 信封，example 为该命令完整必填形态 | PASS |
| G52/G53 | edit 互斥同给/皆缺 | usage exit 2 | 符合 | PASS |
| G54 | 自提及 `--mention alice`（author alice） | validation exit 1 | MJ-2 flag 层拒绝 | PASS |
| G55 | 提及值含空格 `bo b` | validation exit 1 | 拒绝 | PASS |
| G57 | 提及值 `#3`（纯数字形态） | validation exit 1 | 拒绝 | PASS |
| G56 | `--reply-to 999` 指向不存在 seq | exit 0 静默（沿用 v0.5） | `ok post.send #7`，读取时派生 reply_to=999 | PASS |
| E13–E15 | 旧位置文法迁移面（profile/brief/contacts） | usage exit 2 ×3 | 信封示例即 v0.6 形态 | PASS |

**小结**：§2 全表 14 条签名逐一可执行；短形式收窄（仅 `-a/-m/-q`）实测成立；必填缺失一律 usage exit 2 且示例完整；dash 正文直传无需 `--` 边界（含值为 `-a`/`--stdin` 字面的极端形态）。

---

## 3. 错误信封实测

### 3.1 category 与退出码矩阵（E01–E16 + §2 负向）

| 形态 | category | exit | 实测结论 |
|---|---|---|---|
| 漏必填 / 互斥 / 未知 flag / 类型错误 / 多余位置参数 / 旧文法 | usage | 2 | 全部命中，信封含 message+fix+example |
| 空正文 / 空 author / 坏 mention 形态 | validation | 1 | 命中 |
| 文件不存在（post read/summary、profile show、brief read、contacts read） | not-found | 1 | 命中 |
| 空 .post.md / 未闭合围栏 / 未知后缀无 --type / legacy v0.4 线程 send | format | 1 | 命中（legacy 拒绝为 format-v2 合并带来的 M2 防护，实测生效） |
| seq gap（`#1` 直接 `#3`） | validation | 1 | 命中（R10 裁决的直出形态） |
| 重复 create（profile/brief/contacts） | already-exists | 1 | 命中 |
| edit 护栏违规 / 消息 seq 不存在 | not-allowed / not-found | 1 | 命中 |
| validate 不存在文件 | io | 1 | 沿用 v0.5（运行时六类含 io） |

### 3.2 fix + example 可复制执行核验（24 条去重 example 全部真实执行）

- **usage 信封**：每命令一条静态规范示例，具体值无占位符；全部执行成功或语义正确（post send 规范示例恰为 spec §5.2 指定的 `paperwork post send standup.post.md --author alice --message "Hello"`）。
- **运行时信封**：not-found/already-exists/validation 类 example 逐一执行，均为合法命令（exit 0 或语义上的正确运行时结果，无一条落入 usage 错误）。
- **缺陷**：io category 与部分 format/validation 信封缺 example 行（见 §7 BUG-3）；io 错误消息在 zh-CN locale 携带中文 OS 文案（见 BUG-2 附带观察）。
- 首轮自动执行中 4 条例判 FAIL-USAGE，经复核系 QA 脚本对引号多词参数的分词错误；按原样字符串手工重跑全部 exit 0，example 本身无误。

---

## 4. 输出协议冻结回归（spec §5/§7）

16 条协议用例（P01–P16）+ 22 项 ASCII 字节级抽查。

| 冻结项 | 实测 | 判定 |
|---|---|---|
| ok 信封字段（send：seq/path/sender；read：showing/window） | `--json` 与默认形态字段齐；`showing: n/total` + `window: #first-#last` 恒显（含 `-q` 下） | PASS |
| `--json` 只增不改不删 | 既有 key（status/command/conclusion/messages…）原样，read 新增 showing/window 为增项 | PASS |
| `--json` 错误信封 | usage：`exit_code: 2` 如实；运行时：`exit_code: 1` + category/fix/example/command；`--json` 置于子命令之后（argv 扫描兜底）同样生效 | PASS |
| `-q` 语义 | 仅隐 `ok ...` 首行，字段与正文保留；与 `--json` 组合正常 | PASS |
| `--plain` | post read 输出原始文件内容（字节级即文件本体） | PASS |
| reply 隐式 mention | carol 回复 bob 的 #2 → ok 信封 `implicit-mention: bob`；自回复不触发（边界保持） | PASS |
| 派生引用（D2 读取时派生） | `--json` read：`reply_to`/`mentions` 从正文 token 派生，落盘无独立字段 | PASS |
| command 标识 | `post.send/post.read/post.summary/post.edit` 等不变；`post.create` 随 format-v2 删除，未出现 | PASS |
| 纯 ASCII 契约 | 成功信封/`--plain`/summary 等 6 处输出字节级抽查 0 非 ASCII；15 个子命令 help 全扫——**唯 `post send --help` 含 2 处 `§`（BUG-1）** | FAIL×1 |

---

## 5. format-v2 合并行为回归（落盘文件逐一打开核对）

| 行为 | 实测 | 判定 |
|---|---|---|
| 首次 send 建线程 | `f5.post.md` 由 send 创建：preamble + #1 同锁写入 | PASS |
| preamble 仅 H1（D1） | 文件首部仅 `# Frozen Check` 一行，无 participants/属性行 | PASS |
| `--title` 首写生效 | 首写 `--title "Frozen Check"` 落 preamble | PASS |
| 既有线程 `--title` 静默忽略（OQ-1） | 二次 send 附 `--title SHOULD-BE-IGNORED`：exit 0、无信号、文件 H1 未变 | PASS |
| 默认标题派生 | `derived-name.post.md` → `# derived-name`（剥 `.post.md` 规则） | PASS |
| reply-to token 注入落盘形态 | 正文围栏内首行 `@#2`，空行后原正文；读取派生 `reply_to: 2` | PASS |
| mention token 注入落盘形态 | 首行 `@bob @carol`（单空格分隔、同一行）；派生 `mentions: [...]` | PASS |
| 消息边界/动态围栏 | `## #N sender (ts)` + ```md 围栏；edit 重写后格式保持 | PASS |
| brief 条目结构 | H2 = 条目（basename 全名 `alice.profile.md`）+ 属性行（path/hash）+ 裸散文 note | PASS |
| legacy v0.4 线程写防护 | 向含 `---`/`### #N` 构造的文件 send → `error format:` 拒绝（M2 防护经合并保留） | PASS |

---

## 6. 并发回归（多进程实测）

方法：`Start-Process` 并发拉起 N 个 `paperwork.exe post send` 独立进程写同一新线程，等待全部退出后正则提取 seq 集合核验间隙/重复。共 7 轮。

| 轮次 | 配置 | 结果 |
|---|---|---|
| 轮 1 | 10 并发 send | 全 exit 0，seq 1–10 无间隙 **PASS** |
| 轮 2 | 10 并发 send | **1 条 exit 1（io, os error 33），seq 缺 1 条 FAIL** |
| r1/r2 | 各 10 并发 | 各 1 条同型失败 **FAIL×2** |
| r3 | 20 并发 | 2 条同型失败（18/20） **FAIL** |
| 混合读写 | 6 写 + 6 读并发 | 全通过（低概率窗口未触发） PASS |
| race 探测 | 15 写 + 15 读 + 15 summary | 1 写者同型失败 **FAIL** |

**结论：并发不变量（seq 无间隙无重复）在「全部成功」前提下成立，但 Windows 下多进程并发 send 以约 10–20% 概率出现 io 失败并丢消息——升级为 BUG-2（Critical）。** 失败消息未写入、文件无损坏、seq 无重复（失败即整体未提交，原子性保持），但调用方收到 io 错误后消息丢失，须重试。既有并发测试（`concurrent_thread_send_safety` 等）为**同进程线程**模型，且 CI 为 Linux——跨进程 + Windows 锁语义的暴露面未被覆盖。

---

## 7. bug 清单与裁决

### BUG-2 · Critical · Windows 多进程并发 send 间歇失败丢消息（io, os error 33）

**复现**（zh-CN Windows，worktree release 二进制）：

```
# 并发 10–20 个进程对同一新线程执行：
paperwork post send conc.post.md --author agentN --message msg-N
```

**实际输出**（失败者，两种形态，概率约 10–20%）：

```
error io: 另一个程序正在处理文件的一部分，进程无法访问。 (os error 33)
# 或
error io: IO error at '<path>': <同上中文文案> (os error 33)
fix: check file permissions
```

**期望**：并发 send 全部成功（阻塞等锁），seq 1–N 无间隙。

**根因**（已读码闭环）：`cmd/post.rs` 的 `reject_foreign_thread`（post.rs L434–447，send 前置检查，引入自 v0.5 修复轮 `39156db`，经 `a07ad4c` 合并带入）在**任何锁之外**执行两次全文件读（`thread_read` + 裸 `std::fs::read_to_string`）。另一进程此刻正在 `thread_send` 内持有 fs2（0.4.3，`LockFileEx`）排他字节区间锁——Windows 强制锁语义下，**跨进程对被锁区间的读操作立即失败为 ERROR_LOCK_VIOLATION(33)**（不同于锁获取的阻塞等待）。两种信封形态恰对应两处读：裸 `?` 走 `PaperworkError::Io`（无路径上下文），`thread_read` 走 `IoContext`（fix 文案误导为 "check file permissions"）。Linux flock 不阻断读者，故此缺陷为 Windows 专属潜伏问题，v0.5 起存在、本轮实测首曝。
**影响**：多 agent 并发编排（本工具核心场景）下消息间歇丢失（有错误信号、文件不损坏，但需调用方重试）；fix 文案误导。
**建议修复方向**（供 owner 裁决，QA 不改码）：legacy 检查移入锁内（thread_send 已有 contains_legacy_headers 锁内检测，reject_foreign_thread 可退化为锁内职责），或锁外读对 os error 33 做有界重试；并补**多进程**并发回归（Windows 平台 CI）。
**附带观察**：同一锁外读结构理论上同样暴露 post read/summary 与写者的竞争（本轮 45 个读者进程未触发，概率更低但结构相同）；io 信封消息内嵌 zh-CN OS 文案，附带击穿 ASCII 契约（与 BUG-1 同属 §4 契约面）。

### BUG-1 · Major · `post send --help` 泄漏非 ASCII `§`，违反冻结的纯 ASCII 输出契约

**复现**：

```
paperwork post send --help | 字节级扫描
```

**实际输出**：`--reply-to` / `--mention` 两条 help 文本含 `§`（U+00A7，各 2 UTF-8 字节），全 CLI 唯一非 ASCII 输出点（其余 14 个 help 与全部信封抽查均为纯 ASCII）。
**期望**：全部 stdout/stderr 字节 ASCII（spec §5 第 4 条冻结条款、v0.5 确立的 `ascii_output_contract_guard` 级约束）。
**根因**：`d6e9ff3` 在 `repos/paperwork-cli/src/cmd/post.rs` 的 clap doc 注释写入 `spec §11 OQ-4`（post.rs L72/L77 附近）；集成测试防线未覆盖 help 文本扫描。
**建议修复**：`§11` → `sec. 11`（或 `section 11`）；可加一条遍历全部子命令 help 的 ASCII 断言测试。

### BUG-3 · minor · 部分运行时信封缺 example 行

**复现**：`paperwork brief add onboarding.brief.md --entry src/nope.rs`（io）；对 seq gap / 未闭合围栏文件执行 `validate`。
**实际输出**：io category 信封有 fix 无 example；`main.rs` 通用 anyhow io 分支 fix/example 均空；validate 的 seq gap 与 unclosed fence 信封有 fix 无 example。
**期望**：信封模板（v0.5 spec §4.2）三行齐备；至少 io 类给出可执行示例。
**定性**：v0.5 既有（非 v0.6 引入），usage/validation/not-found/already-exists 信封均合规。

### BUG-4 · minor（文档）· spec §2 `brief add` 签名未显值参数

**事实**：spec §2/§3.5 写作 `[--regex] [--note]`（开关形态记法），实测两者均为带值 flag（`--regex <REGEX>` 缺值落 usage exit 2，help 明示）。v0.5 spec 同为该简写，行为两版一致，属记法承继瑕疵。
**建议**：下轮规格修订时改写为 `[--regex <PATTERN>] [--note <TEXT>]`。

### 发布就绪判定

**判定：不满足「可供 owner 验收的稳定候选」。**

- BUG-2（Critical）为**必经修复项**：并发写是 agent 协作核心场景，Windows（owner 环境）下可复现丢消息，且既有测试模型（同进程线程 + Linux CI）无法覆盖；修复后须补多进程并发回归再测。
- BUG-1（Major）违反逐条冻结的输出协议，且为 v0.6 本轮新引入，建议同批修复。
- BUG-3/BUG-4（minor）不阻塞验收，可另立批次。
- 其余 151 项实测全部通过：文法全表、信封机制、协议冻结、format-v2 合并行为、测试基线（218 全绿 = CLI 集成 92 + core 75 + ops 51，`cargo test --workspace`）均无回归。

**实测统计**：共 157 项（文法 64 + 信封 16 + example 执行 24 + 协议 16 + format-v2 落盘 7 + 并发 7 轮 + ASCII 抽查 22 + 测试基线 1），通过 151，失败 6（并发失败轮 5 + ASCII help 1）。bug 分级：**Critical ×1、Major ×1、minor ×2**。

---

*End of QA review book.*

---

## 修复回应（统一修复轮销账，2026-08-09，实施者追加）

门禁复核：`cargo test --workspace` 239 全绿（CLI 集成 113 + core 75 + ops 51）；`cargo clippy --workspace --all-targets -- -D warnings` 零警告；ops_tests.rs 零改动。

| 编号 | 状态 | 修复回应 |
|---|---|---|
| BUG-2（Critical，多进程并发丢消息） | 已销账 | 根因修复：`reject_foreign_thread` 重写为**锁内读取**——`File::open` + `lock_exclusive()` 后通过同一句柄读取（Windows 强制锁语义下跨句柄读被锁区间必失败，故读必须走持锁句柄），改用 `parse_messages` 单次解析，消除原 `thread_read` + 裸 `read_to_string` 双读；锁/seq 分配语义不变（thread_send 内部逻辑零改动）。新增**多进程**并发回归测试 `multiprocess_concurrent_send_no_lost_messages`（assert_cmd 起 10 个 paperwork 进程并发 send 同一新线程，断言全成功、seq 1..=10 连续无缺、消息数=10、sender/body 逐一核对，Windows/Linux 双平台可跑）。实测复跑：10 进程×5 轮全 PASS（每轮 ok=10、messages=10、seq=[1..10]），0 失败。 |
| BUG-1（Major，`§` ASCII 泄漏） | 已销账 | post.rs 两处 `§` 全部改为 `section`（d6e9ff3 引入点）；全量 grep help/after_help/usage 文案确认 repos/paperwork-cli 用户可见字符串无非 ASCII（残留仅 core 注释/测试数据）；新增 `all_help_output_is_pure_ascii` 防线：遍历 root + 5 命令组 + 16 子命令 --help 逐字节断言 ≤0x7F。 |
| BUG-3（minor，io/format/validation 信封缺 example） | 已销账 | main.rs 通用 anyhow io 分支补 fix + example；validate.rs 的 seq gap 与 unclosed fence 信封补 example；core 侧 hash 失配（brief add io 类）与 thread seq monotonicity 信封补 example；均 v0.6 具名形态可执行。 |
| BUG-4（minor，spec brief add 记法） | 已销账 | spec §2/§3.5 与 design 的 brief add 记法改为显式带值 `[--regex <PATTERN>] [--note <TEXT>]` 并注明勘误依据；主工作区 docs 对应文件已同步修正（仅 docs/）。 |
| 附带观察（io 信封 zh-CN OS 文案） | 登记备查 | io 错误消息内嵌 OS locale 文案属 std::io 错误透传，修复超本轮范围（需 OS 错误码映射层）；BUG-2 本体已消除，该附带观察不阻塞验收，留待后续轮次处置。 |

遗留：0（附带观察 1 项登记备查，非阻塞）。

