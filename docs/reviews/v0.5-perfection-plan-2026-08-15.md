# v0.5 完美收口：全债务清零与深度修复计划（落盘版）

**性质**: owner 指令驱动的 v0.5 遗留债务清零计划（**零悬置**）——功能 / 结构 / 安全性 / 正确性相关的漏洞与 BUG 全部完整修复，不允许任何阻断项或悬置推迟项。
**日期**: 2026-08-15
**前置评审书**: [`docs/reviews/v0.5-full-review-2026-08-14.md`](v0.5-full-review-2026-08-14.md)（§3.4 债务台账为本计划的权威来源）
**规格基线**: `docs/dev/format-v2/{spec,design,bdd,tdd,impl_plan}.md`、79 BDD 场景

> **owner 指令原文要旨**：
> “不论是功能、结构、安全性、正确性相关的漏洞 BUG，都应该被完整修复……没有任何阻断项或者遗留悬置推迟项……来保证当前最新版的 paperwork 的完美。”

本指令直接推翻前置评审书 §3.4 的全部“裁决留档 / 延期”处置：Noah DRY 五项（原裁决“留 v0.6”）、Lucas m6–m10（原裁决“不修”）、Ethan S1（原裁决“仅补文档”）、Ivy G1–G5（原裁决“留后续批次”）一律升级为本轮闭合项。三路并行规划（Sam 简洁可维护 / Tina 性能安全 / Eric 最小变更）已完成并交叉核验；本计划以 Eric 的“行为锁定先行 + 逐点等价替换 + 每步全量门禁”绞杀式骨架为基础，并入 Tina 的全部 NEW-1~NEW-13 深挖修复与 Sam 的 LockedFile / brief 残留守卫 / 台账重建方案。

**铁约束**：格式规格（spec.md）、CLI 文法、ASCII 信封输出契约零变更（JSON 键名与键序逐字节冻结）；公开 Rust API 签名与 re-export 不变；不 bump 版本（保持 0.5.0）；`docs/ssot/specs/cli-grammar-v0.6/` 与 cli-ux-redesign 未提交变更零触碰。

**闭合通则**（适用于全部台账项）：

1. 每项只有两种合法终态：**修复**（配回归测试或 CLI 实证），或 **核实无需改 + 实测证据**（n13 先例）；不存在第三种状态，不产生新的悬置。
2. 所有行为新增（守卫 / 护栏）须在 CHANGELOG Unreleased 显式披露；所有 Rust API 变更（如删除死变体）须单独披露小节。
3. 每个执行阶段完成后回流更新本文档对应台账项的闭合状态，保持文档与执行现场单一事实源。

---

## §1 债务对照台账（对前置评审 §3.4 逐项闭合）

| # | 原台账项（来源视角） | 原裁决（2026-08-14） | 本轮闭合方式 | 对应执行任务 |
|---|----------------------|---------------------|--------------|--------------|
| 1 | Noah：fence 状态机重复（评审时点约 9 处；本轮实测 11 处） | 留 v0.6 | **修复**：共享 fence 扫描器收口（终态口径（Ultra Review F6 订正）：7 处行级位点全迁移；4 处位点以成文豁免共享谓词——两位字节级 + 两内容收集型（含 #3/#5 高风险面），配 differential 测试） | T3 / T4 |
| 2 | Noah：消息头族正则 4 变体 | 留 v0.6 | **修复**：集中 format 层同址 + 删除 SEQ_RE 双正则冗余（直接走 `header_seq` 谓词） | T3 / T4 |
| 3 | Noah：IoContext 样板约 31 处 | 留 v0.6 | **修复**：`io_ctx` helper + `LockedFile` RAII 守卫归零；5 处 RMW 锁序列统一，消除手工 unlock 早退路径（含 NEW-13） | T3 / T4 |
| 4 | Noah：手工 JSON 拼装约 9 处 | 留 v0.6 | **修复**：黄金快照锁定后统一 output.rs builder（键序由 `serde_json::Map` 决定——未启用 `preserve_order`，即 BTreeMap 字母序，与替换前输出逐字节一致；Ultra Review F4 订正原"插入序"误述） | T1 / T6 |
| 5 | Noah：ops/thread.rs 体量偏大（评审时点约 647 行，实测已 780 行） | 留 v0.6 | **修复**：拆为 thread.rs（send/edit 编排）+ thread_read.rs（read/summary）+ thread_scan.rs（legacy 守卫 / 尾扫），`pub use` 再导出保 API 不变 | T5 |
| 6 | Lucas：m6–m10 低收益项 | 不修 | **二选一闭合**：m6 引证前轮已修；m7→NEW-9/NEW-11 性能修复；m8→删除 `PaperworkError::Io` 死变体；m10 分项裁决（重命名改 doc 注释映射 / 点号键冻结核实 / snippet_of 双反转核实）。每项留痕不悬置 | T2 / T4 / T8 |
| 7 | Ethan：S1（迁移指南认知面与代码面核对） | 仅补文档 | **修复 + 补守卫**：CHANGELOG 四条迁移指南与代码守卫逐条核对入档；本轮补 brief 部分迁移守卫后认知面与代码面全对齐；MSRV 仅裁决留痕 | T2 / T7 / T8 |
| 8 | Ivy：测试缺口 G1–G5 | 留后续批次 | **重建核实（超集闭合）**：原明细未入库，以证据重建为超集；纯新增测试 + 79 BDD 场景差分表落盘 | T7 |
| — | 增量：Tina 深挖 NEW-1~NEW-13、Sam 补充五项 | （前置评审未覆盖） | **全修**，明细见 §3、§4 | T2 / T4 / T5 / T6 |

---

## §2 Noah DRY 债务实测清单（含位点）

> 前置评审 §3.4 / §2.3 为评审时点口径（fence 约 9 处、thread.rs 约 647 行）；本轮逐文件重扫后刷新为下表实测位点，作为 T3/T4 的迁移清单。

### 2.1 fence 状态机重复（实测 11 处）

| # | 位点 | 函数 / 职责 | 迁移形态 |
|---|------|-------------|----------|
| 1 | `format/thread.rs:103-122` | `header_indices`（消息头行号扫描） | 行级，全迁移共享扫描器 |
| 2 | `format/thread.rs:127-145` | `first_header_index`（首消息头短路） | 行级，全迁移 |
| 3 | `format/thread.rs:246-286` | `parse_message_body`（fence 内外判定） | 谓词共享（高风险面裁决，见 §8.1 T4 行） |
| 4 | `format/manifest.rs:53-72` | `h2_indices` | 行级，全迁移 |
| 5 | `format/manifest.rs`（同上邻域） | `parse_entry_body` | 谓词共享（高风险面裁决，见 §8.1 T4 行） |
| 6 | `format/contacts.rs:57-78` | `contains_bare_bullet`（B1 守卫） | 行级，全迁移 |
| 7 | `format/mod.rs:162-189` | `validate_markdown` | 行级，全迁移 |
| 8 | `ops/thread.rs:594-630` | `first_message_header_offset`（**字节级**） | 共享 fence 谓词、保留字节循环 |
| 9 | `ops/thread.rs:638-675` | `contains_legacy_headers` | 行级，全迁移 |
| 10 | `ops/thread.rs:687-780` | `read_last_seq_locked`（**字节级**） | 共享 fence 谓词、保留字节循环 |
| 11 | `cli/cmd/validate.rs:147-179` | `suspected_header_warnings` | 行级，全迁移 |

共享扫描器族设计（T3 落点 `format/mod.rs`）：`for_each_outside_fence`（早停回调）+ `first_outside_fence` / `collect_outside_fence`，复用既有 `fence_open_len` / `fence_close_matches` 谓词。两个字节级位点（#8、#10）**豁免迭代器共享，仅共享谓词**，配 differential 对拍测试（CRLF / 缩进 fence / tilde / 断 fence / 孤立 `\r` 语料）。

### 2.2 头族正则 4 变体

| 正则 | 位点 | 收口方式 |
|------|------|----------|
| `MESSAGE_HEADER_RE` | `format/thread.rs:30` | 集中至 format 层同址，作为 SSOT |
| `SEQ_RE` | `ops/thread.rs:41` | **删除**双正则冗余，直接走 `header_seq` 谓词 |
| `LEGACY_HEADER_RE` | `ops/thread.rs:47` | 集中归族 |
| `SUSPECTED_HEADER_RE` | `cmd/validate.rs:22` | 终态（Ultra Review F7 订正）：**裁决留 CLI**——CLI 专属警告启发式 + 空白立场差异成文 + 避免扩大 core 公开面；成文位置 `format/thread.rs:33-52` 头族注释块 |

### 2.3 IoContext 样板与 RMW 锁序列

- IoContext 样板约 **31 处**，分布于 ops 四文件（thread / profile / manifest / contacts）+ `cmd/validate.rs` → `io_ctx` helper + `LockedFile` RAII 守卫归零（NEW-13 一并收口）。
- **5 处 RMW 锁序列**统一为单一 helper 路径，消除手工 unlock 的早退路径。

### 2.4 手工 JSON 9 处

| 位点 | 位点 | 位点 |
|------|------|------|
| `output.rs:64` | `output.rs:104` | `cmd/post.rs:258` |
| `cmd/post.rs:331` | `cmd/brief.rs:148` | `cmd/brief.rs:161` |
| `cmd/brief.rs:247` | `cmd/contacts.rs:90` | `cmd/profile.rs:139` |
| `cmd/profile.rs:292` | | |

共 9 处命令侧拼装 + 2 处协议层（保留）。黄金快照锁定后统一 output.rs builder，保持 `serde_json::Map` 插入序（键序逐字节冻结），**不采用 derive struct**（见 §8 被拒方案 1）。

### 2.5 ops/thread.rs 体量

实测 **780 行**（评审时点约 647 行）。拆分方案：thread.rs（send/edit 编排）+ thread_read.rs（read/summary）+ thread_scan.rs（legacy 守卫 / 尾扫），`pub use` 再导出保公开 API 不变（T5）。

---

## §3 深挖新发现 NEW-1~NEW-13（Tina 视角，全部本轮修复）

| 编号 | 标题 | 定位与问题 | 闭合方式 | 任务 |
|------|------|------------|----------|------|
| NEW-1 | 注入护栏：单行字段无换行校验 | `serialize_preamble` title（`format/thread.rs:313`）、profile name/model、contacts title/label/path、manifest title/owner 均为单行字段但写侧不拒 `\n`/`\r`；更危险者：profile description prose 含 `- model:` 同形行时，RMW 重解析会**抢占真实 model**（`parse_profile` 取首个，`format/profile.rs:63-66`） | 单行字段写侧拒 `\n`/`\r`；prose 危险同形行防护泛化为 `note_representation_issue`；配双向用例（拒注入不误伤合法多行 prose） | T2 |
| NEW-2 | create TOCTOU | `ops/profile.rs:25`、`ops/manifest.rs:38`、`ops/contacts.rs:29` 均为 `exists()+write` 两段式，存在竞争窗口 | 改 `create_new(true)` 原子创建；配 N 线程竞争测试 | T2 |
| NEW-3 | ensure_suffix lossy | `cmd/mod.rs:24` 使用 `to_string_lossy`，非法 UTF-8 路径被静默替换字符 | 改 OsStr 原生拼接 | T6 |
| NEW-4 | contacts read 富化与 derive_label 两级解析不一致 | `ops/contacts.rs:195-203`（read 富化）与 `cmd/contacts.rs:136-142`（derive_label）各自解析，语义漂移风险 | 共享解析 helper | T2 |
| NEW-5 | parse_contacts_title 非 fence 感知 | `format/contacts.rs:13-28` 不跳过 fence 内内容，fence 内伪 title 可被误纳 | 改 fence 感知 | T2 |
| NEW-6 | 尾扫 fence 奇偶限制无测试钉住 | `ops/thread.rs:752-777` 尾扫在 fence 奇偶边界上的行为无测试固化 | 测试钉住 + 文档化立场 | T2 |
| NEW-7 | hash_file 全量读 | `hash.rs:22-30` 一次性读入全文件计算哈希 | 改流式 SHA-256（BufReader 分块） | T5 |
| NEW-8 | thread_edit 全文重写 | `ops/thread.rs:546-573` 每次 edit 重写全文件 | 改末条增量重写（truncate 至末头偏移 + 追加）；与全重写字节一致对照测试（CRLF / preamble 伪头 / fence 内假头语料） | T5 |
| NEW-9 | normalize 双趟 + validate 重复归一化 | `format/mod.rs:22-27`，validate 路径重复归一化 3–4 次 | 单趟 normalize + 顶层归一一次 Cow 下传 | T4 |
| NEW-10 | participants/mentions 去重 O(n²) | `ops/thread.rs:321-326`、`format/thread.rs:66-76` 使用 `Vec::contains` | HashSet + Vec 保序 | T5 |
| NEW-11 | hex_encode 逐字节 format! | `hash.rs:33-35` 32 次 `format!` 调用 | 单趟编码 | T5 |
| NEW-12 | reply-to 隐式 mention 双读全文件 | `cmd/post.rs:181-193` 为查询 reply 目标全量重读文件 | 改尾扫式目标查询 | T5 |
| NEW-13 | 解锁路径不一致 | 手工 unlock 存在早退路径分歧 | 由 `LockedFile` RAII 一并收口（并入 §2.3 / A3） | T3/T4 |

---

## §4 Sam 补充发现（全部本轮修复）

| 编号 | 标题 | 定位与问题 | 闭合方式 | 任务 |
|------|------|------------|----------|------|
| SAM-1 | brief 部分迁移守卫边缘洞（Ethan S1 代码面） | 小写键但残留 `## Entries` / H3 的 brief 可**通过解析**并被 RMW 破坏（与 B1 同构：解析忽略 + RMW 重写） | `format/manifest.rs` 解析层残留检测 + `ops/manifest.rs` 写路径拒绝 + validate 同步报 format 错误；仅按结构位置特征触发，配负例测试 | T2 |
| SAM-2 | profile create 带 scope 双开文件双加锁 | `cmd/profile.rs:100-124` 同一文件开两次、锁两次 | 改单次写入 | T2 |
| SAM-3 | `default_title` 与 `derive_label` suffix-stripping 重复 + 三处 Vec::contains 去重 | 重复逻辑分散 | core 共享 helper 收口 | T4 |
| SAM-4 | `verify_entry` 吞 IO 错误为 Stale | 读取失败被误报为 hash 不匹配（Stale） | 区分 hash 不匹配与读取失败 | T2 |
| SAM-5 | 死代码 `PaperworkError::Io` 变体 | `error.rs:40`，实测无显式构造 | 先 grep 确认无 `?` 隐式转换依赖，有则显式 IoContext 化后删除；CHANGELOG 披露 Rust API 变更 | T8 |

---

## §5 Ivy G1–G5 重建台账（原明细未入库，以证据重建为超集）

前置评审 §3.4 仅存 G1–G5 结构化摘要，原始明细未入库。本轮以可核证据重建为超集，闭合方式统一为 **T7 纯新增测试 + 79 BDD 场景差分表落盘**，不改既有 176 测试：

| 编号 | 重建内容 | 覆盖面 |
|------|----------|--------|
| G1 | VAL-04：v0.4 旧格式（`### #N` 形态）CLI 坏例 | validate 对旧 thread 格式报 `error format:` |
| G2 | VAL-05：三格式坏例 CLI 面——profile 缺 model / brief 缺 owner/created / contacts legacy | validate 机器检查下限三格式坏例全谱 |
| G3 | validate `--json` 错误信封断言 | 错误路径 JSON 键名键序冻结 |
| G4 | post edit 三拒绝（非本人 / 非最近 / 非末条）CLI 信封 | 拒绝语义在 ASCII 信封面逐项断言 |
| G5 | post read 过滤 + summary 缺失文件宽容 + 错误路径 quiet/plain/json 组合 + CRLF/Unicode CLI 面 | 宽容路径与输出组合矩阵 |

超集增量（并入 T7）：注入护栏回归（NEW-1 双向用例）、brief 部分迁移守卫负例（SAM-1）、并发 create 竞争（NEW-2）、四格式 roundtrip 恒等语料库（含 `test-v05/` 九文件）、JSON 形状快照（9 处 payload）、README 测试计数同步新基线、**79 BDD 场景 × 测试矩阵差分表落盘**。

---

## §6 Lucas m6–m10 重建台账（逐项二选一闭合，不留悬置）

原明细未入库。本轮以 Sam m-α~m-δ、Tina NEW 候选为超集重建，每项二选一闭合：**修复** 或 **核实无需改 + 实测证据**（n13 先例），不允许悬置。每项在 T8 落盘最终闭合结论。

| 编号 | 重建候选 | 本轮闭合 |
|------|----------|----------|
| m6 | 时间解析收口（parse_timestamp 双份实现） | **已在前轮 M6 修复**（收口 `format/mod.rs` + `RFC3339_FMT` 常量），本轮引证闭合 |
| m7 | 性能项候选 = NEW-9（normalize 双趟）/ NEW-11（hex_encode 逐字节） | **本轮修复**（T4/T5） |
| m8 | 死代码候选 = `PaperworkError::Io` 死变体 | **本轮删除**（T8，先 grep 隐式 `?` 依赖） |
| m10 | 分项：① Manifest 类型重命名 → **Rust API breaking，裁决改 doc 注释映射闭合**（同 n13 先例）；② JSON 点号键 → **属输出契约冻结，不“修正”**，以“核实无需改 + 证据”闭合；③ `snippet_of` 双反转 → 核实行为，若正确则以证据闭合 |
| （m9） | 明细缺失 | 以超集覆盖兜底，T8 留痕说明 |

---

## §7 Ethan S1 闭合

S1 原裁决“仅补文档，不改代码”。本轮按 owner 指令升级为**认知面与代码面全对齐**：

1. **逐条核对**：CHANGELOG 迁移指南四条与代码守卫一一对照——post legacy 拒写（`ops/thread.rs:129-136`）、contacts 裸 bullet 拒写（`format/contacts.rs:57`）逐条核对结论入档（T8）。
2. **补洞**：本轮补 brief 部分迁移守卫（SAM-1）后，迁移指南认知面与代码守卫面**全对齐**——三类格式的部分迁移形态均有写侧守卫。
3. **MSRV 裁决**：README 宣称 Rust 1.89+，**仅裁决留痕，不加 `rust-version` 字段**（加字段属行为面变更候选，owner 未授权；见 §8 被拒方案 4）。

---

## §8 执行链与门禁

### 8.1 T0–T11 依赖链

| 阶段 | 内容 | 依赖 |
|------|------|------|
| **T0 台账与计划落盘** | 本文档（文档先行，**未落盘不开工**） | — |
| **T1 行为锁定** | 新增 `repos/paperwork-cli/tests/char_tests.rs` 与 core 侧特征化测试：黄金信封快照（固定 fixture 跑全部命令面 default/json/plain/quiet，stdout/stderr 逐字节）、错误文案冻结（B1/M1/M2/n4 等 message+fix+example 全文）、四格式 roundtrip 恒等语料库、JSON 形状快照（9 处 payload）。门禁：新测试全绿、既有 176 不动 | T0；与 T2 并行 |
| **T2 正确性护栏批（core 为主）** | NEW-1/2/3/4/5/6 + brief 部分迁移守卫（SAM-1）+ profile create 单次写入（SAM-2）+ verify_entry 错误区分（SAM-4）。每项配回归测试（并发 create 竞争、注入双向用例、守卫负例） | T0；与 T1 并行 |
| **T3 共享基础设施（纯新增）** | `format/mod.rs`：共享 fence 扫描器族、单趟 normalize、单行字段/prose 校验 helper；`error.rs` 邻位或 `ops/mod.rs`：`io_ctx` helper + `LockedFile` RAII；头族正则集中 format/thread.rs | T1 基线 |
| **T4 core 逐点迁移（严格串行）** | 顺序：头族正则归族/删 SEQ_RE → IoContext 31 处 + RMW 5 处锁序列（每批一文件）→ fence 低风险面 → 高风险面（parse_entry_body / parse_message_body / read_last_seq_locked 谓词共享）→ dedup/suffix helper 收口。每处先 differential 对拍后删旧码 | T3 |
| **T5 ops/thread.rs 拆分 + 性能批** | 按 §2.5 拆分（`pub use` 保 API）；随后 NEW-8 增量重写、NEW-7/10/11/12 | T4 |
| **T6 CLI JSON 收口** | output.rs builder 替换 8 处命令侧位点（协议层 2 处保留）；validate.rs 消除重复归一化；ensure_suffix OsStr 修复（NEW-3） | T1 快照；与 T4/T5 不同 crate 可并行 |
| **T7 Ivy 测试缺口闭合** | §5 全部新增测试 + BDD 差分表落盘 + README 计数同步 | T2 / T6 |
| **T8 CI 与文档** | ci.yml 增 `cargo doc --no-deps`（并入 fmt job）、test job 加 `--locked`；CHANGELOG Unreleased 小节（内部重构声明、守卫行为新增、`PaperworkError::Io` 删除披露、Known downgrade 复核）；Lucas/Ethan 闭合台账落盘 | 与 T4–T7 并行 |
| **T9 终验门禁** | 独立 Verify 复刻评审书 §4 十三项 QA + 黄金快照总比对 + `cargo clean` 后 clippy + fmt --check + release CLI 实证 + B1 SHA256 零字节复验 | 全部 |
| **T10 Ultra Review** | 三 CodeReview 并行（completeness / correctness / impact），发现回流修复直至零 Critical/Major | T9 |
| **T11 提交推送** | 自主 Git：按归属分批提交（修复 / 测试 / 文档），排除 v0.6 工作流文件，普通 push origin master | T10 |

### 8.2 每步验证门禁

每步 = `cargo test --workspace` 全绿（**176 旧测试原样存活** + 累计新增）+ 黄金快照零 diff + `cargo fmt --check`；T4 每处迁移另加 differential 对拍；**T9 为总门禁**。

### 8.3 风险表

| 风险 | 对策 |
|------|------|
| 错误文案 / JSON 字节漂移 | T1 黄金快照逐字节断言；io_ctx/builder 逐处传参保留原措辞，禁止统一措辞 |
| fence 扫描器语义漂移（字节偏移 / 短路 / lone-`\r` 变体） | 字节级两位点豁免迭代器共享、仅共享谓词；每处先对拍后删旧 |
| create_new 跨平台错误映射差异 | CI 三平台矩阵 + 竞争测试 |
| 注入护栏误伤合法多行 prose | 护栏只拒单行字段换行与 prose 危险同形行，双向用例钉住 |
| thread_edit 增量重写边界错误 | 与全重写字节一致对照（CRLF / preamble 伪头 / fence 内假头语料） |
| 删除 `PaperworkError::Io` 触发隐式 `?` 编译错误 | 先 grep 全量 `?` 依赖点显式化，再删 |
| 长链重构中途失败 | 每步独立提交独立门禁，爆炸半径 ≤ 一批位点，可单点回滚 |

### 8.4 被拒方案（五条）

1. **Tina 的 JSON derive struct 方案**：字段声明序可能与现状键序不同，破坏下游 agent 消费者；改用 Map 插入序保持的 builder + 黄金快照。
2. **Sam 的 Manifest→Brief 类型重命名**：Rust API breaking 且收益仅术语一致；改 doc 注释映射闭合（同 n13 先例）。
3. **Eric 对字节级 fence 变体的完全豁免**：与“零悬置”指令冲突；折中为共享谓词不共享迭代器，differential 测试兜底。
4. **新增 CI MSRV job（rust-version 字段）**：属行为面变更候选，owner 未授权；仅裁决留痕。
5. **perf smoke 设时间阈值**：CI 抖动误报风险；只断言正确性并记录耗时。

### 8.5 关键文件与假设

**关键文件**：`repos/paperwork-core/src/ops/thread.rs`（780 行，DRY 与拆分焦点）、`repos/paperwork-core/src/format/mod.rs`（共享原语与扫描器落点）、`repos/paperwork-core/src/format/thread.rs`（正则 SSOT、parse/serialize 基线）、`repos/paperwork-cli/src/output.rs`（JSON 收口中心）、`repos/paperwork-cli/tests/cli_integration.rs`（行为门禁基准）、`docs/reviews/v0.5-full-review-2026-08-14.md`（§3.4 台账权威）。

**假设**：
- 行为契约冻结仅对合法 v0.5 文件；新增守卫 / 护栏属行为新增，CHANGELOG Unreleased 显式披露。
- 版本保持 0.5.0 不 bump（owner 尚未发布，沿用前轮约定）。
- Ivy G1–G5 与 Lucas m6–m10 原始明细不在仓库，以证据重建超集闭合，闭合文档留痕（T8）。

---

## §9 附录：证据文件清单与计数基线

### 9.1 本计划引用的证据文件

| 类别 | 路径 | 用途 |
|------|------|------|
| 评审权威 | `docs/reviews/v0.5-full-review-2026-08-14.md` | §3.4 债务台账权威来源（§1 对照表右列） |
| 代码（core format） | `repos/paperwork-core/src/format/{thread,manifest,contacts,profile,mod}.rs` | §2.1/§2.2 fence 位点与头族正则、NEW-1/NEW-5/SAM-1 定位 |
| 代码（core ops） | `repos/paperwork-core/src/ops/{thread,profile,manifest,contacts}.rs` | §2.1 字节级位点、NEW-2/NEW-8/NEW-10/NEW-12、SAM-2/SAM-4 定位 |
| 代码（core 杂项） | `repos/paperwork-core/src/{error,hash}.rs` | SAM-5 死变体（error.rs:40）、NEW-7/NEW-11（hash.rs） |
| 代码（cli） | `repos/paperwork-cli/src/{output.rs,cmd/{post,brief,contacts,profile,validate,mod}.rs}` | §2.4 手工 JSON 九处、NEW-3/NEW-12/SAM-2 定位 |
| 测试基线 | `repos/paperwork-cli/tests/cli_integration.rs`（41）、`repos/paperwork-core/tests/ops_tests.rs`（59）、core lib 内联（76） | 176 基线；T1 新增 char_tests.rs 不改既有 |
| CI | `.github/workflows/ci.yml` | T8 CI 补齐对象（cargo doc / --locked） |
| 文档 | `CHANGELOG.md`、`README.md` | S1 迁移指南四条核对对象（§7）；T7 计数同步对象 |
| 规格 | `docs/dev/format-v2/{spec,design,bdd,tdd,impl_plan}.md` | 铁约束冻结基线；79 BDD 场景差分表（T7） |
| 语料 | `test-v05/`（9 文件，未入库） | T1 roundtrip 恒等语料库成员 |

### 9.2 计数基线（执行前快照）

| 指标 | 数值 | 出处 |
|------|------|------|
| 测试总量 | 176（core 内联 76 + ops_tests 59 + cli_integration 41） | 前置评审书 §1.4 |
| BDD 场景 | 79（PROF 11 / POST 36 / BRIEF 12 / CONT 8 / CONC 4 / VAL 8） | 前置评审书 §1.4 |
| ops/thread.rs 行数 | 780（本轮实测，评审时点约 647） | §2.5 |
| fence 状态机位点 | 11（本轮实测，评审时点约 9） | §2.1 |
| IoContext 样板 | 约 31 处；RMW 锁序列 5 处 | §2.3 |
| 手工 JSON | 9 处命令侧 + 2 处协议层保留 | §2.4 |
| 深挖新增项 | NEW-1~NEW-13（13 项）+ Sam 五项 | §3 / §4 |

注：上表为 T0 落盘时点的执行前快照；T9 终验时须重取实测值并与本表对照，差异须留痕。

---

## §9 执行终态索引（Ultra Review 回流批追加）

各台账项 → 闭合 commit / 文档引证（一页速查；详述见各文档）：

| 台账项 | 终态 | 引证 |
|--------|------|------|
| T1 行为锁定（char_tests + core 特征化） | 闭合 | Olivia 批 commit；`repos/paperwork-cli/tests/char_tests.rs` + core roundtrip 语料 |
| T2 正确性护栏批 | 闭合 | Max 批 commit（NEW-1~6 / SAM-1~5 配回归测试） |
| T3 共享基础设施 | 闭合 | Adam 批 commit；`format/mod.rs` 扫描器族 / `ops/lock.rs` / `io_ctx` |
| T4 core 逐点迁移 | 闭合 | Zoe 批 commit；fence 口径差异留痕见台账 §3.1 |
| T5 thread.rs 拆分 + 性能批 | 闭合 | `0842522`（拆分）/ `47958aa` / `a9b68a0` / `ecc2c23` |
| T6 CLI JSON 收口 | 闭合 | Ruby 批 commit；`output.rs` JsonBuilder（键序机制订正见 F4） |
| T7 Ivy 缺口闭合 | 闭合 | `5bfb061`（G1–G5，16 测，304→320） |
| T8 CI 补齐与台账闭合 | 闭合 | `b6cc2d4`；F3 回流再补 RUSTDOCFLAGS（`8539a08`） |
| T9 终验门禁 | 闭合 | Owen 批；320 全绿 + clippy/fmt/doc 零告警 |
| Lucas m6–m10 / Ethan S1 | 闭合 | 台账 §1 / §2 |
| Noah 头族收口终态 | 闭合 | 台账 §3.2；`format/thread.rs:33-52` |
| F1 写读对称守卫 | 修复闭合 | `633a6fe` |
| F2 strip 超集拆分 | 修复闭合 | `ffb7a54` |
| F3 doc 门禁实効化 | 修复闭合 | `8539a08`（ci.yml RUSTDOCFLAGS） |
| F4 JSON 键序误述 | 修复闭合 | `8539a08`（output.rs doc + 单测）+ CHANGELOG |
| F5 窗口披露 + 锁拓扑表述 | **裁决闭合**（不下沉锁内，披露闭合） | 台账 §3.3 + CHANGELOG `[Unreleased]` |
| F6 fence 计数口径 | 修复闭合（口径订正） | 台账 §3.1 + 本文 §1/§2.1 + CHANGELOG |
| F7 文档批 | 修复闭合 | 台账 §3.2 / test-matrix §8.3 订正 / `ops/profile.rs` doc / 本索引 |
| F8 Rust API 披露 | 披露闭合 | CHANGELOG `[Unreleased]` Added |
| F9 Drop 注释 + S-3 | S-3 **裁决闭合（不实现，信封形状冻结）** | 台账 §3.4 |

---

*本台账与计划为 T0 交付物；后续 T1–T11 以本文档为唯一执行依据，任何偏离须回流修订本文档后方可动工。*
